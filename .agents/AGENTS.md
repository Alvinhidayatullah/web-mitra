# GitHub Auto-Sync Rule

- Whenever making any code improvements, bug fixes, or functionality updates in this repository, ALWAYS execute `git add .`, `git commit -m "[Description of changes]"`, and `git push` immediately after verifying that the changes work. This ensures the GitHub repository remains continuously synchronized with local improvements.
